<p id="notice"><%= notice %></p>

<h1>Migrations</h1>

<table>
  <thead>
    <tr>
      <th>Add afteractiveunitid to activeflow</th>
      <th>Afteractiveunit</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @migrations.each do |migration| %>
      <tr>
        <td><%= migration.add_afteractiveunitid_to_activeflow %></td>
        <td><%= migration.afteractiveunit_id %></td>
        <td><%= link_to 'Show', migration %></td>
        <td><%= link_to 'Edit', edit_migration_path(migration) %></td>
        <td><%= link_to 'Destroy', migration, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Migration', new_migration_path %>
